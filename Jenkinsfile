pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                    echo'building lala'
            }
        }
    }

    post {
        success {
            echo 'Docker image build successful!'
        }
    }
}
